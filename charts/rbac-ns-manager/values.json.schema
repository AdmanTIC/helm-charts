{
  "$schema": "http://json-schema.org/schema#",
  "type": "object",
  "required": [
    "publicApiURL"
  ],
  "properties": {
    "publicApiURL": {
      "type": "string"
      "pattern": "^[a-z0-9-_]+$"
    }
  }
}